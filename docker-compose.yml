version: '3.8'

services:
  gatsby-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "9000:9000"
    environment:
      - NODE_ENV=production
      - PORT=9000
    volumes:
      - ./examples/gatsby-minimal-starter/public:/app/public
    restart: unless-stopped 